spring:
  application:
    name: gateway
  profiles:
    default: eureka-on
server:
  port: 4040
eureka:
  client:
    fetch-registry: true
    server-url:
      defaultZone: http://localhost:8761/eureka/